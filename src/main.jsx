import React from "react"
import ReactDOM from "react-dom/client"
import ChildHealthDashboard from "./ChildHealthDashboard"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChildHealthDashboard />
  </React.StrictMode>
)
